// example of functions
// .toFixed
// Number()
// max()
// prompt()
// min()

// function is a block of JavaScript code that
// can accept inputs and provide an output after processing the input

console.log(Math.max(2,3));  // Math.max() is a function that accepts
                             // 2 or more parameters and the result is
                             // the largest of all the parameters
// when we 'use' a function, we write the name of function followed
// by parenthesis -- CALLING A FUNCTION AKA FUNCTION CALL
console.log(Math.min(-5, -100, 200)); // we 'pass' -5, -100 and 200 as the PARAMETERS

// a function when called executed some internal process
// on the parameters and returns a result. The return value
// of a function its results
console.log(Math.max(1,2)); // <- call Math.max and pass 1 and 2 to it
                            // and the return value is 2
                            // or Math.max(1,2) returns 2